{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Iniciar o Programa",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/dist/server.js", // Certifique-se de que este é o seu ponto de entrada transpilado
      "outFiles": ["${workspaceFolder}/dist/**/*.js"], // Inclui todos os arquivos transpilados
      "sourceMaps": true
    }
  ]
}
